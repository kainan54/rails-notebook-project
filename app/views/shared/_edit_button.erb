<% if !!current_user %>
    <% if current_user.owns_note_book(note_book.id) %>
            <%= button_to "Edit", edit_note_book_path(note_book.id), class: "nb-edit-btn", method: :get %>
    <% end %>
<% end %>